{
  "Logging": {
    "LogLevel": {
      "Default": "Debug"
    }
  },
  "_comment_LocalOverrides": "The base appsettings.json's Identity/Inventory/Payment values are portless container-DNS defaults for Docker/kart-devops; these override them to each service's own local dev port (its launchSettings.json http profile) for a bare-metal `dotnet run` against locally-running services.",
  "Identity": {
    "JwksUri": "http://localhost:8081/.well-known/jwks.json"
  },
  "Inventory": {
    "BaseUrl": "http://localhost:8086"
  },
  "Payment": {
    "BaseUrl": "http://localhost:8089"
  }
}
